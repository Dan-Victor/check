export const metadata = {
  title: `Product page | Checkit`,
  description: "Product detail page",
};
export default function ProductPage({children}: {children: React.ReactNode}) {
  return <div>{children}</div>;
}